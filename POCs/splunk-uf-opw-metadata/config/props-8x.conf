# props.conf for 8.x transforms test UF
# force_local_processing = true tells the UF to run these transforms locally
# rather than deferring to a downstream indexer.

[poc_8x]
force_local_processing = true
TRANSFORMS-embed-meta = embed-index, embed-sourcetype, embed-source, embed-host
